#include <stdio.h>

int main()
{
   int age;
       printf("enter the age : ");
       scanf ("%d", &age);
    if(age > 16){
       printf ("ticketPrice = 20") ;
    }
    else {
       printf ("ticketPrice = 10") ;
    }
}
