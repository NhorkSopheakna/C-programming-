#include <stdio.h>

int main() {

    // Variable number is the number for user enter
    int num;
    printf("Enter the number: ");
    scanf("%d", &num);

    //first condition if user enter number under 10
    if  (num <10) {
        printf("Too low", num);
    }

    //Second condition if user enter number from 10 to 20
    else if (num<=20){
        printf("Correct",num);
    }

    // Third condition if user enter number more than 20
    else
    printf ("Too high");

    return 0;
}
